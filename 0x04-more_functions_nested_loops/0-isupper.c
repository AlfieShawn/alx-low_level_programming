#include<stdio.h>
#include<main.h>
/**
*main - Check for uppercase
*
*Return: Always 0 (Success)
*/
int _isupper(int c);
{
	char c;

	if (c >= 'A' && c <= 'Z')
	{
		Return(1);
	}
	else
	{
		Return(0);
	}
}
